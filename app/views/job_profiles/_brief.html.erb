<h1 class="govuk-heading-xl govuk-!-margin-bottom-2">How to become: <%= @job_profile.name.downcase %></h1>
<% @job_profile.additional_hero_copy.each do |paragraph| %>
  <p class="govuk-body-l"><%= paragraph %></p>
<% end %>
<% if @job_profile.alternative_titles.present? %>
  <p class="govuk-body-l muted-text">Other similar jobs include: <%= @job_profile.alternative_titles.downcase %></p>
<% end %>
